
<ul>
<% @items.each do|item| %>
  <li>
    <%= item.id %>
    <%= item.num %>
    <%= item.cost %>
    <%= item.created_at.in_time_zone("Asia/Tokyo") %>
    <%= item.updated_at.in_time_zone("Asia/Tokyo") %>
  </li>
<% end %>
</ul>